---
description: Generate the Python structure (skill mri-scaffold-python) → /mri-implement
---

Invoke the `mri-scaffold-python` skill (uv + ruff + pytest + mypy, `src/` layout), following
`.mri_devtools/constitution.md`.

$ARGUMENTS

Next step → **`/mri-implement`**.
💡 Suggested model: **Sonnet / DeepSeek-v4** (mechanical task). See `.mri_devtools/models.md`.
