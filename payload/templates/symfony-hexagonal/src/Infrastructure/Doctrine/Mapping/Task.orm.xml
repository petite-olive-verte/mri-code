<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">
    <!--
      Mapping for the pure domain model App\Domain\Model\Task. Keeping it in XML
      here (Infrastructure) is what lets the entity carry no ORM attributes.
    -->
    <entity name="App\Domain\Model\Task" table="tasks">
        <id name="id" type="task_id" column="id">
            <generator strategy="NONE"/>
        </id>
        <field name="title" type="string" length="255"/>
        <field name="status" type="string" length="16" enum-type="App\Domain\ValueObject\TaskStatus"/>
        <field name="createdAt" type="datetime_immutable" column="created_at"/>
    </entity>
</doctrine-mapping>
