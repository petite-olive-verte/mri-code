---
description: Générer la structure Python (scaffold-python) → suggère /implement
---

Invoque la skill `scaffold-python` pour générer la structure du projet à partir de
`.toolbox/templates/python-uv/`, en respectant `.toolbox/constitution.md`. Vérifie que `uv sync`,
`pytest` et `ruff` sont verts.

$ARGUMENTS

À la fin, indique : « Prochaine étape → /implement ».
