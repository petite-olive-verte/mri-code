---
description: Plan technique + tâches (writing-plans) → suggère /scaffold ou /implement
---

Invoque la skill `superpowers:writing-plans` à partir de la spec (`docs/specs/<projet>/spec.md`).
Produis un plan technique et des **tâches ordonnées avec cases à cocher** dans
`docs/specs/<projet>/plan.md` et `tasks.md`. Présente le plan pour validation (plan mode).

$ARGUMENTS

À la fin, indique la suite : « Nouveau projet → /scaffold ; sinon → /implement ».
